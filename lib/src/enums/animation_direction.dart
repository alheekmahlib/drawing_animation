/// Defines the direction of the animation.
enum AnimationDirection {
  /// Animation starts from left to right.
  leftToRight,

  /// Animation starts from right to left.
  rightToLeft,

  /// Animation follows the original path order.
  original,
}
